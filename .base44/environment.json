{
  "previewPort": 3000,
  "healthPath": "/api/health",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "VITE_BASE44_APP_ID",
      "description": "Base44 app ID from the Base44 dashboard (found in the URL between /apps/ and /editor/). Set this when publishing the app through Base44. When empty, the app falls back to localStorage for data persistence."
    }
  ]
}
